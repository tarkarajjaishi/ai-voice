# v7.4.0 Agent Tools Validation Matrix

Release gate for Agent-scoped transfer, Google Calendar, Microsoft Calendar, and voicemail
resources; immutable runtime generations; Context removal; and the three-Agent first-run
experience. Results below are from the feature branch and the candidate deployed on both
the development node and voiprnd on 2026-07-16. Two maintainer-supervised calendar calls
provide the PR spot check; the broader destructive/denied-scope matrix remains a pre-tag
release gate rather than an unrecorded claim.

## Candidate

- **Branch:** `v7.4.0-agent-tools`
- **Base:** `origin/main` at `d8916cf7` (`v7.3.5`)
- **Development server:** `10.44.0.104`
- **Live validation target:** `voiprnd.nemtclouddispatch.com:3003`

## Automated and static gates

| Gate | Expected | Status |
|---|---|---|
| Full engine Python suite | No regressions | PASS — 1,600 passed, 7 skipped |
| Full Admin backend suite | No regressions | PASS — 426 passed |
| Agent policy/runtime/reload focused tests | transfer/calendar/account/mailbox inherit/selected/none, inherited/selected placeholder and stale/empty fail-closed, global voicemail disable at execution, invalid stored-policy call-start failure, generation isolation, reload rollback/409 | PASS |
| Legacy import tests | atomic complete import, bundled-demo exclusion, collisions, durable completion marker, raw-placeholder drift-hash parity/null backfill, malformed tool-policy rejection, invalid rollback, populated/deleted-store preservation | PASS |
| Starter/pipeline Agent tests | exactly Receptionist/Sales/Support, Receptionist default, idempotent, compatible starter provider and direct pipeline-only profile resolution, blank-input defaults, rollback on default-write failure | PASS |
| Frontend Vitest | Agent policy round-trip, executable transfer-inventory filtering, donation milestone/cadence, and existing UI tests | PASS — 180 passed |
| Frontend lint | No warning-regression beyond pinned baseline | PASS — 867 warnings, 0 errors (limit 868) |
| Frontend production build | TypeScript/Vite build completes | PASS |
| Diff hygiene | no whitespace errors; unrelated user files untouched | PASS |

## Development-server gates

| Area | Procedure | Status |
|---|---|---|
| Upgrade migration | Start with legacy Contexts and empty Agent store; verify complete import and default | PASS — deployed image imported atomically, retained canonical selected-transfer policy, chose the expected default, passed integrity check, and was idempotent |
| Existing Agent safety | Start with populated `agents.db`; verify zero overwrite/reseed | PASS — development data remained 12 rows/1 default/12 active; schema was upgraded additively and automatic Context import did not run |
| Removed Context UI | Confirm no sidebar/palette entry; `/contexts` notice appears once then redirects | PASS — production browser flow verified locally; deployed bundle contains the notice and removed navigation |
| Empty first run | Complete wizard; verify exactly three Agents and no transfer routes | PASS — isolated production browser flow created Receptionist, Sales, Support only, with Receptionist default |
| Agent tool UI | Configure inherit, selected, selected-empty, stale, and none policies | PASS — production browser flow verified migrated badges plus selected Google and Microsoft resources, voicemail mailbox policy, transfer policy, and fail-closed messaging; automated inventory coverage excludes null/non-object transfer placeholders from the picker |
| Global voicemail compatibility | Load the legacy single `extension`, expose it without schema loss, and offer named-mailbox conversion | PASS — production browser flow showed extension 2765 and the additive “Add another mailbox” path without saving or mutating live configuration |
| Calendar Context compatibility | Import legacy Context calendar bindings without allowing Context overlays to broaden an Agent scope | PASS — migration, runtime-overlay, already-populated-store, and API reconciliation tests pass; six voiprnd demo Agents were migrated to explicit selected Google/Microsoft resources |
| Call History compatibility | Preserve canonical tool names and existing execution rendering | PASS — tests cover `google_calendar`, `microsoft_calendar`, and `leave_voicemail`; production Call History loaded 1,779 calls and rendered a four-execution call detail successfully |
| Save & Apply | Change a managed HTTP/built-in tool; verify generation/hash increments without restart | PASS — development engine published generation 1 then generation 2 with 15 tools and no restart |
| Active-call isolation | Keep a call active across reload; verify it retains old schema/config and next call gets new | PRE-TAG LIVE GATE — automated generation-isolation tests pass; not exercised during the two-call PR spot check |
| Failed reload | Submit invalid tool configuration; verify previous generation remains active | PASS — atomic rollback and concurrent 409 behavior covered by focused tests; destructive config mutation was not applied to the shared development server |
| Restart boundary | Change MCP/env/provider setting; verify UI reports restart required | PASS — focused backend/UI coverage and development reload response identify provider changes as restart-bound |
| Browser console/API | No new console errors; Agent CRUD and reload endpoints return expected status | PASS — authenticated public UI, Agent resources, Tools, Call History, catalog/config/Agent APIs, and live status verified on voiprnd; server health remained green with zero active calls |

## Maintainer-supervised PR call evidence

Raw engine/Admin logs were archived before analysis under `logs/archived/` using the
archive-first call-analysis workflow.

| Call | Provider / Agent | Tool evidence | Result |
|---|---|---|---|
| `1784237941.482` | Google Live / `demo_google_live` | Agent selected Google calendar key `calendar_1`; two `get_free_slots` calls and one `create_event` succeeded; Call History recorded the executions; email summary and post-call SMS completed | PASS — completed normally in 81.55 s |
| `1784238037.486` | OpenAI Realtime / `demo_openai` | Agent snapshot selected Google `calendar_1`, Microsoft `default`, and voicemail `default`; two `get_free_slots` calls and one `create_event` succeeded; Call History recorded the executions; post-call SMS completed | PASS — completed normally in 153.58 s |

Both calls used tool generation 5. The maintainer confirmed the caller-visible behavior
was correct with no issue. The first OpenAI availability query contained past candidate
times; the calendar provider corrected it and the successful booking path was unaffected.

## Pre-tag live-call regression gates on voiprnd

Run at least one selected-scope transfer and one denied-scope attempt on both AudioSocket
and ExternalMedia where configured. Also run allowed and denied Google Calendar, Microsoft
Calendar, and voicemail calls, including one stale or selected-empty fail-closed case. Confirm
provider schema, spoken guidance, execution result, canonical tool names in Call History,
audit generation, cleanup, and post-call hooks all agree. Then run the configured
provider/pipeline golden baseline set to exclude greeting, barge-in, farewell, and transfer
regressions. Archive call evidence before marking these rows PASS.

The pre-tag gates intentionally cover the only behavior that cannot be proven without calls
in progress: generation retention across a reload and end-to-end allowed/denied resource
behavior. The PR may be reviewed with the two recorded spot checks plus automated coverage;
do not mark the unexecuted transfer, Microsoft Calendar, voicemail, denied-scope, or
active-call-reload scenarios PASS until their call IDs are archived.
